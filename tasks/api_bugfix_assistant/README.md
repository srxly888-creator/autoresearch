# API Bugfix Task

This task is designed for API-first users.

Goal:
- Automatically fix bugs in a small OpenAI-compatible API helper.
- Validate with a deterministic local evaluator (no network calls).

Editable file:
- `workspace/api_client.py`

Evaluator:
- `evaluate.py`

Run:

```bash
python3 run_task.py --description baseline
```
